<p id="notice"><%= notice %></p>

<h1><%= t('activerecord.models.train') %></h1>

<table>
  <thead>
    <tr>
      <th><%= t('activerecord.attributes.train.number') %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
  <%= render @trains %>
  </tbody>
</table>

<br>

<%= link_to t('common.refferences.new'), new_admin_train_path %>
