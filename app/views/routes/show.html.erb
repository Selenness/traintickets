<p id="notice"><%= notice %></p>

<p>
  <strong>Название</strong>
  <%= @route.name %>
</p>

<p>
  <strong>Поезда на маршруте</strong>
  <% if @route.trains.present? %>
      <% @route.trains.each do |tr| %>
          <div><%= tr.number %></div>
      <% end %>
    <% else%>
      Нет поездов
    <% end %>
</p>
<p>
  <strong>Список станций</strong>
  <% if @route.railway_stations.empty? %>
      Cтанций в маршруте нет
  <% else %>
    <table>
      <thead>
      <tr>
        <th>Номер станции</th>
        <th>Станция</th>
      </tr>
      </thead>
      <tbody>
      <% @route.railway_stations_routes.each do |st| %>
      <tr>
        <td><%= st.serial_number %></td>
        <td><%= st.railway_station.name %></td>
      </tr>
      <% end %>
      </tbody>
    </table>
  <% end %>
</p>

<%= link_to 'Редактировать', edit_route_path(@route) %> |
<%= link_to 'Назад', routes_path %>
